# Secrets — never commit
.env
.env.*
*.token

# SigMap-generated artifacts (regenerated by `npx sigmap`)
.context/
.github/copilot-instructions.md

# Python
__pycache__/
*.pyc
.venv/

# demo video — upload to YouTube, not the repo
*.mp4

# Internal prep — not for the public repo / judges
DEMO_SCRIPT.md
YOUTUBE.md
SUBMISSION.md
